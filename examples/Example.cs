using System;

class Program {
  static void Main(string[] args) {
    // Console.WriteLine("CommentCleaner is a vS Code Extension to clean up comments in your code");
    Console.WriteLine("CommentCleaner is a VS Code Extension to clean up comments in your code");
  }
}
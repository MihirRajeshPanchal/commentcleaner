#include <stdio.h>

int main() {
    // printf("CommentCleaner is a vS Code Extension to clean up comments in your code");
    printf("CommentCleaner is a VS Code Extension to clean up comments in your code");
    return 0;
}
